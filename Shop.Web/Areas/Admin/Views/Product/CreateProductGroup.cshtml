@model Shop.Domain.ViewModels.Admin.Product.CreateProductGroupViewModel
@{
    ViewData["Title"] = "افزودن گروه جدید";
}

<meta name=robots content=noindex,nofollow>

<div class="row">
    <form asp-area="Admin" asp-controller="Product" asp-action="CreateProductGroup" method="post" enctype="multipart/form-data">
        <div class="col-md-8">
            <h2>@ViewData["Title"]</h2>
            <hr />
            <div class="form-group">
                <label>عنوان </label>
                <input type="text" asp-for="Title" class="form-control">
                <span asp-validation-for="Title"></span>
            </div>
            <div class="form-group">
                <label>عنوان یکتا</label>
                <input type="text" asp-for="UrlName" class="form-control">
                <span asp-validation-for="UrlName"></span>
            </div>
            <input type="submit" value="ذخیره اطلاعات" class="btn btn-success" />
        </div>
        <div class="col-md-4">
            <p></p>
            <img id="imGroup" class="thumbnail" src="/images/shopping-cart.png" />
            <div class="form-group">

                <label>انتخاب تصویر</label>
                <input type="file" name="imageGroup" id="imgProductGroup">
            </div>
        </div>
    </form>
</div>

@section Scripts
{
    <partial name="_Custom" />
    <partial name="_ValidationScriptsPartial" />
}